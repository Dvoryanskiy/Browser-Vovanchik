# Инструкция по запуску браузера на Windows

## Шаг 1: Установка Node.js

1. **Скачайте Node.js:**
   - Перейдите на сайт: https://nodejs.org/
   - Скачайте LTS версию (рекомендуется)
   - Выберите установщик для Windows (`.msi` файл)

2. **Установите Node.js:**
   - Запустите скачанный `.msi` файл
   - Следуйте инструкциям установщика
   - **Важно:** Убедитесь, что опция "Add to PATH" отмечена
   - Нажмите "Install"

3. **Проверьте установку:**
   - Откройте PowerShell или командную строку
   - Выполните команды:
   ```powershell
   node --version
   npm --version
   ```
   - Должны отобразиться версии (например, v20.x.x и 10.x.x)

## Шаг 2: Установка зависимостей проекта

1. **Откройте PowerShell или командную строку**

2. **Перейдите в папку проекта:**
   ```powershell
   cd C:\Users\Ivan\my-browser
   ```

3. **Установите зависимости:**
   ```powershell
   npm install
   ```
   Это установит Electron и другие необходимые пакеты (может занять несколько минут)

## Шаг 3: Запуск браузера

### Обычный режим:
```powershell
npm start
```

### Режим разработки (с открытыми инструментами разработчика):
```powershell
npm run dev
```

## Альтернативный способ запуска (если npm start не работает):

```powershell
npx electron .
```

## Возможные проблемы и решения:

### Проблема: "node не является внутренней или внешней командой"
**Решение:** 
- Перезапустите PowerShell/командную строку после установки Node.js
- Или добавьте Node.js в PATH вручную:
  - Откройте "Переменные среды" в Windows
  - Добавьте путь к Node.js (обычно `C:\Program Files\nodejs\`)

### Проблема: Ошибки при npm install
**Решение:**
- Убедитесь, что у вас есть интернет-соединение
- Попробуйте запустить PowerShell от имени администратора
- Очистите кэш npm: `npm cache clean --force`

### Проблема: Антивирус блокирует установку
**Решение:**
- Временно отключите антивирус
- Или добавьте папку проекта в исключения

## Горячие клавиши в браузере:

- `Ctrl+T` - Новая вкладка
- `Ctrl+W` - Закрыть вкладку
- `Alt+←` - Назад
- `Alt+→` - Вперед
- `Ctrl+R` - Обновить страницу
- `F11` - Полный экран

## Что дальше?

После успешного запуска вы увидите окно браузера. Вы можете:
- Вводить URL в адресную строку
- Вводить поисковые запросы (автоматически будет использован Google)
- Создавать новые вкладки
- Навигироваться по истории

Удачи! 🚀
